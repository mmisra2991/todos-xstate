# Getting Started with Create React App

Actors
- internal state
- send message (events)
- Receive message, change its "behivour" based on that msg
- spawn other actors

import {createMachine} from 'xstate;